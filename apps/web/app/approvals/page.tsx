import { StubPage } from '@/components/StubPage';

export default function ApprovalsPage() {
  return (
    <StubPage
      title="Approvals"
      phase="Phase 4 (Test Planning)"
      description="Action-specific, time-bounded, revocable approvals for mutating tests, patches, and repository writes will be reviewed here."
    />
  );
}
