import { StubPage } from '@/components/StubPage';

export default function DiscoveryPage() {
  return (
    <StubPage
      title="Discovery"
      phase="Phase 1 (Project & Repository Discovery) and Phase 2 (Docker Compose Discovery)"
      description="Repository stack, frameworks, containers, ports, routes, and existing test tooling will be shown here with confidence levels."
    />
  );
}
