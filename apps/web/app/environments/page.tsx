import { StubPage } from '@/components/StubPage';

export default function EnvironmentsPage() {
  return (
    <StubPage
      title="Environments"
      phase="Phase 1 (Project & Repository Discovery)"
      description="Classify each environment (local, development, test, staging, production) and configure mutation/load-test/security-scan permissions here."
    />
  );
}
