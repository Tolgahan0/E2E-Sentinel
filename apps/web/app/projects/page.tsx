import { StubPage } from '@/components/StubPage';

export default function ProjectsPage() {
  return (
    <StubPage
      title="Projects"
      phase="Phase 1 (Project & Repository Discovery)"
      description="Add a local project, validate its root path, and view repository scan results here."
    />
  );
}
