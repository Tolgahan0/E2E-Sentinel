import { StubPage } from '@/components/StubPage';

export default function AiProvidersPage() {
  return (
    <StubPage
      title="AI Providers"
      phase="Phase 6 (AI Providers)"
      description="Configure Ollama, OpenAI, Anthropic, Gemini, Azure OpenAI, or an OpenAI-compatible endpoint here. E2E Sentinel remains fully usable with AI disabled."
    />
  );
}
