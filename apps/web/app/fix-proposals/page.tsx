import { StubPage } from '@/components/StubPage';

export default function FixProposalsPage() {
  return (
    <StubPage
      title="Fix Proposals"
      phase="Phase 8 (Fix Proposals)"
      description="AI-generated patch diffs, risk explanations, and the approve/apply-to-workspace workflow will live here. Patches are never applied without explicit approval."
    />
  );
}
