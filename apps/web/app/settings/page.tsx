import { StubPage } from '@/components/StubPage';

export default function SettingsPage() {
  return (
    <StubPage
      title="Settings"
      phase="Phase 6 (AI Providers) and Phase 9 (Production Hardening)"
      description="Storage, retention, RBAC, and other administrative settings will be configured here."
    />
  );
}
