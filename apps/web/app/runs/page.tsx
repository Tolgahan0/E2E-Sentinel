import { StubPage } from '@/components/StubPage';

export default function RunsPage() {
  return (
    <StubPage
      title="Runs"
      phase="Phase 5 (Playwright Runner)"
      description="Live logs, screenshots, video, traces, HAR, and service logs for each isolated test run will appear here."
    />
  );
}
