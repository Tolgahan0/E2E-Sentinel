import { StubPage } from '@/components/StubPage';

export default function ApplicationMapPage() {
  return (
    <StubPage
      title="Application Map"
      phase="Phase 3 (Application Graph)"
      description="A zoomable, evidence-backed graph connecting UI routes, APIs, services, and infrastructure will render here."
    />
  );
}
